create table kirim_berkas
(
  id Bigint UNSIGNED NOT NULL AUTO_INCREMENT,
  tanggal date NOT NULL,
  foto_berkas_dan_bukti VARCHAR(30),
  pengeluaran_id VARCHAR(30),
  created_at timestamp default current_timestamp, 
  updated_at timestamp,
  PRIMARY KEY (id)
);


create table petugas_kirims
(
  id Bigint UNSIGNED NOT NULL AUTO_INCREMENT,
  staf_id VARCHAR(30) NOT NULL,
  role_pengiriman_id VARCHAR(30) NOT NULL,
  kirim_berkas_id VARCHAR(30) NOT NULL,
  created_at timestamp default current_timestamp, 
  updated_at timestamp,
  PRIMARY KEY (id)
);

ALTER TABLE piutang_asuransis
ADD COLUMN kirim_berkas_id VARCHAR(30);

create table role_pengirimans
(
  id Bigint UNSIGNED NOT NULL AUTO_INCREMENT,
  role_pengiriman VARCHAR(30) NOT NULL,
  created_at timestamp default current_timestamp, 
  updated_at timestamp,
  PRIMARY KEY (id)
);

insert into role_pengirimans ( role_pengiriman, created_at, updated_at) Values ( 'petugas pengiriman', NOW(), NOW() );
insert into role_pengirimans ( role_pengiriman, created_at, updated_at) Values ( 'supervisi', NOW(), NOW() );
