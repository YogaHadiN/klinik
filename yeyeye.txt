CREATE TABLE data_duplikats(
  id INT NOT NULL AUTO_INCREMENT,
  no_telp varchar(255),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW() ON UPDATE NOW(),
  created_at TIMESTAMP NOT NULL,
  PRIMARY KEY(id)
);

CREATE TABLE peserta_bpjs_perbulans(
  id INT NOT NULL AUTO_INCREMENT,
  nama_file varchar(255),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW() ON UPDATE NOW(),
  created_at TIMESTAMP NOT NULL,
  PRIMARY KEY(id)
);

ALTER TABLE antrians ADD nomor_bpjs varchar(255);
ALTER TABLE pasiens ADD prolanis_ht tinyint(1) not null default 0;
ALTER TABLE pasiens ADD prolanis_dm tinyint(1) not null default 0;
ALTER TABLE pasiens ADD panggilan varchar(255) not null;

update pasiens set sex = 0 where sex = 'P';
update pasiens set sex = 1 where sex = 'L';

update pasiens set sex = 0 where nama like '%,ny' or nama like '%, ny';
update pasiens set sex = 1 where nama like '%,tn' or nama like '%, tn';

=========================================================================================
CREATE TABLE admedika_perbulans(
  id INT NOT NULL AUTO_INCREMENT,
  nama_file varchar(255),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW() ON UPDATE NOW(),
  created_at TIMESTAMP NOT NULL,
  PRIMARY KEY(id)
);
